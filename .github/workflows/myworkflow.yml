name: MyWorkFlow
on: push
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome Message
          run: echo 'Welcome Message'
        - name: list of files
          run: ls  
